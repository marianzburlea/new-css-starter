# create folders helper command

```
mkdir "css-home"
mkdir "css-introduction"
mkdir "css-syntax"
mkdir "css-selectors"
mkdir "css-how-to"
mkdir "css-comments"
mkdir "css-colors"
mkdir "css-colors"
mkdir "css-rgb"
mkdir "css-hex"
mkdir "css-hsl"
mkdir "css-backgrounds"
mkdir "background-color"
mkdir "background-image"
mkdir "background-repeat"
mkdir "background-attachment"
mkdir "background-shorthand"
mkdir "css-borders"
mkdir "border-width"
mkdir "border-color"
mkdir "border-sides"
mkdir "border-shorthand"
mkdir "rounded-borders"
mkdir "css-margins"
mkdir "margin-collapse"
mkdir "css-padding"
mkdir "css-height-width"
mkdir "css-box-model"
mkdir "css-outline"
mkdir "outline-width"
mkdir "outline-color"
mkdir "outline-shorthand"
mkdir "outline-offset"
mkdir "css-text"
mkdir "text-color"
mkdir "text-alignment"
mkdir "text-decoration"
mkdir "text-transformation"
mkdir "text-spacing"
mkdir "text-shadow"
mkdir "css-fonts"
mkdir "font-family"
mkdir "font-web-safe"
mkdir "font-fallbacks"
mkdir "font-style"
mkdir "font-size"
mkdir "font-google"
mkdir "font-pairings"
mkdir "font-shorthand"
mkdir "css-icons"
mkdir "css-links"
mkdir "css-lists"
mkdir "css-tables"
mkdir "table-borders"
mkdir "table-size"
mkdir "table-alignment"
mkdir "table-style"
mkdir "table-responsive"
mkdir "css-display"
mkdir "css-max-width"
mkdir "css-position"
mkdir "css-z-index"
mkdir "css-overflow"
mkdir "css-float"
mkdir "css-float"
mkdir "css-clear"
mkdir "float-examples"
mkdir "css-inline-block"
mkdir "css-align"
mkdir "css-combinators"
mkdir "css-pseudo-class"
mkdir "css-pseudo-element"
mkdir "css-opacity"
mkdir "css-navigation-bar"
mkdir "css-navbar"
mkdir "vertical-navbar"
mkdir "horizontal-navbar"
mkdir "css-dropdowns"
mkdir "css-image-gallery"
mkdir "css-image-sprites"
mkdir "css-attr-selectors"
mkdir "css-forms"
mkdir "css-counters"
mkdir "css-website-layout"
mkdir "css-units"
mkdir "css-specificity"
mkdir "css-important"
mkdir "css-math-functions"
mkdir "css-rounded-corners"
mkdir "css-border-images"
mkdir "css-color-keywords"
mkdir "css-gradients"
mkdir "linear-gradients"
mkdir "radial-gradients"
mkdir "conic-gradients"
mkdir "css-shadows"
mkdir "shadow-effects"
mkdir "box-shadow"
mkdir "css-text-effects"
mkdir "css-web-fonts"
mkdir "css-a2d-transforms"
mkdir "css-a3d-transforms"
mkdir "css-transitions"
mkdir "css-animations"
mkdir "css-tooltips"
mkdir "css-style-images"
mkdir "css-image-reflection"
mkdir "css-object-fit"
mkdir "css-object-position"
mkdir "css-masking"
mkdir "css-buttons"
mkdir "css-pagination"
mkdir "css-multiple-columns"
mkdir "css-user-interface"
mkdir "css-variables"
mkdir "the-var-function"
mkdir "overriding-variables"
mkdir "variables-and-javascript"
mkdir "variables-in-media-queries"
mkdir "css-box-sizing"
mkdir "css-media-queries"
mkdir "css-mq-examples"
mkdir "css-flexbox"
mkdir "css-flex-container"
mkdir "css-flex-items"
mkdir "css-flex-responsive"
mkdir "rwd-intro"
mkdir "rwd-viewport"
mkdir "rwd-grid-view"
mkdir "rwd-media-queries"
mkdir "rwd-images"
mkdir "rwd-videos"
mkdir "rwd-frameworks"
mkdir "rwd-templates"
mkdir "grid-intro"
mkdir "grid-container"
mkdir "grid-item"
mkdir "sass-tutorial"
mkdir "css-templates"
mkdir "css-examples"
mkdir "css-quiz"
mkdir "css-exercises"
mkdir "css-certificate"
mkdir "css-reference"
mkdir "css-functions"
mkdir "css-reference-aural"
mkdir "css-web-safe-fonts"
mkdir "css-animatable"
mkdir "css-px-em-converter"
mkdir "css-color-values"
mkdir "css-default-values"
mkdir "css-browser-support"
```
